# Flatpack Environment Variables — copy to .env and adjust.
APP_URL=http://localhost:8000

FLATPACK_APP_NAME=Flatpack

# When no Laravel policy exists for a model, allow form/list mutations if the user
# can access the panel (true). Set to false for fail-closed. Leave unset for default true.
# Avoid an empty value (KEY= with nothing after =) — that is treated like "unset" here.
# FLATPACK_ALLOW_WHEN_POLICY_MISSING=false

# Dev server (npm run dev)
FLATPACK_VITE_HOST=127.0.0.1
FLATPACK_VITE_PORT=5174

# Where Vite writes `build/` + `hot` for the host app (directory that will contain build/ and hot).
# Default if unset: ./public in this package (assets in public/build).
# For local dev against a host app, use a path to that app’s published tree, e.g.:
# FLATPACK_PUBLIC_DEST=../your-app/public/vendor/flatpack
# (not .../vendor/flatpack/build — that folder is created by Vite.)
# FLATPACK_PUBLIC_DEST=